# ML Enhancement Dependencies for Nura Memory Service
# Install these for advanced ML-based filtering instead of regex patterns

# Core ML framework
transformers>=4.21.0
torch>=1.12.0

# Optional: For faster CPU inference
# accelerate>=0.20.0

# Optional: For even faster inference on supported hardware
# optimum[onnxruntime]>=1.9.0

# Note: The system will automatically fall back to regex patterns 
# if these dependencies are not installed 